/* plugins {
  id "com.moowork.node" version "1.3.1"
}
 */
apply plugin: "com.moowork.node"
apply plugin: 'base'
 
task appNpmBuild(dependsOn: 'npmInstall', type: NpmTask) {
    args = ['run', 'build']
}
 
assemble.dependsOn(appNpmBuild)
